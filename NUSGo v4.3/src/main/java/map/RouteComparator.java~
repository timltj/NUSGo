package Map;

import java.util.Comparator;

public class RouteComparator implements Comparator<Route> {

    @Override
    public int compare(Route route, Route other) {
        if(route.getDuration() > other.getDuration()) return 1;
        else return -1;
    }
}
